life cyclus hooks after_setup_theme, init
which default urls get generated
write your own CPT
how to deactive CPT archive and what else must be done - done by CPT Plugin, but check how it can be done without plugin
sanitizer, escapes..welche für was und wann verwendent man
Bilder generieren mit id, thumbnails, alt, width, height nochmal die Funktionien anschauen
attributes: {
		"width": {
			"type": "string",
			"source": "attribute",
			"selector": "img",
			"attribute": "width"
		},
}
